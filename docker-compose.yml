services:
  api:
    build: .
    env_file:
      - .env
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    environment:
      - PORT=${PORT:-3000}
      - CSV_PATH=/app/backend/data
      - NODE_ENV=${NODE_ENV:-production}
